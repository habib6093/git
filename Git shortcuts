
Use HTTP url to push


git clone https://github.com/habib6093/python.git

copy and paste your files into cloned folder

git add .
git commit -am "commited all files"

git remote add origin https://github.com/habib6093/python.git            // add remote to push 

git push origin master 
git push origin master --force   // if normal push doesn't work


git remote rm origin  // origin is remote url name..


git checkout -b <branch_name>  //create new branch locally 

git push orgin <branch_name>  //push to branch_name.If branch_name doesn't exist it creates new branch or push to existing one

git pull origin master //to update local server

